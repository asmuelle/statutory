{
  "name": "@statutory/core",
  "version": "0.1.0",
  "private": true,
  "description": "Pure deterministic domain logic: normalization, section hashing, diffing, span verification, effective dates, taxonomy, profile matching. No IO, no models.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.0.0",
    "typescript": "^6.0.3"
  }
}
